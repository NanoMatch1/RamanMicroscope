I'd like you to create a new web-app based version of this microscope controller. The machine is on the other side of the world, and Windows Remote Desktop Connection as a diagnostic and operation interface is heavy. This also opens the opportunity to run diagnostics during acquisition, and have clean displays and interfaces. I still want to maintain the old local version, so if it makes sense to do this strangler-fig style, making a headless version that can receive commands and report to the web server/UI, that would be great. My ideal case would be that the user is unaware of the changes until I tell them there's a web-app version they can use to interface. This might need clever wrappers or decorators because we're still developing the hardware interfaces, and there will be new features in the future, so we still want development to be as simple as writing code for the original version, or even simpler. That way, ideally we can improve remote access and monitoring with the web-app while avoiding complications with the dev. Consider my requests, push back where infeasible, propose an implementation plan.
